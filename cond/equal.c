#include <stdio.h>

void main()
{
  int a, b;

    printf("Enter two values : ",a, b);
    scanf("%d%d",&a,&b);

    if(a == b)
        printf("The numbers are same");
    else
        printf("The numbers are not same");

}
